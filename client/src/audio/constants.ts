export const FADE_MS = 300;

export const MAX_DISTANCE = Math.SQRT2;

// closest
export const MIN_DISTANCE = 0.1;
// within this radius a voice is at full volume and fully dry.
export const REF_DISTANCE = 0.1;

// distance
export const MIN_GAIN = 0.02;
export const DISTANCE_CURVE = 2.5;

// size
export const SIZE_MIN_GAIN = 0.1;
export const SIZE_CURVE = 1;

// reverb - based on distance
export const WET_MAX = 0;

export const IMPULSE_URL = "/audio/samples/ir.wav";
